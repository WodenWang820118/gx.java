import { Route } from '@angular/router';

/**
 * Application routing configuration.
 * Currently empty as the app uses a single-page layout with the trading component.
 * Can be extended in the future to add additional routes if needed.
 */
export const appRoutes: Route[] = [];
